I now have the complete audit (11 agents, full design-system inconsistency matrix with file:line evidence). Here is the decision-ready plan. Per your "plan only" choice, I'll touch no code until you pick.

---
Verdict

This site is already high-craft and genuinely premium — bounce-free editorial motion, single-warm-key-light art direction, the paper-deck occlusion philosophy, French micro-typography (U+202F narrow no-break spaces), and real reduced-motion discipline. It is at or near the Linear/Stripe bar. It does not broadly read as AI-generated.

The gap to that tier is not creative, it's systematization: the design language is enforced by convention, not tokens, so the same value (a near-black, a paper-white, a kicker, a Didot floor, a radius, an easing) is re-invented as a literal in 5–10 places and quietly drifts. Plus there are ~4 genuinely slop elements to remove. This is refinement, not redesign.

On your "animated background morph": resolved — it does not exist. There is no blob, morphing gradient, or background morph anywhere in the codebase. The only literal "morph" is the JourneyThread compass→orb (your named concern). Everything else a reviewer might flag is motivated and on-brand (film grain, static blueprint grid, content text-crossfades, solid-color parallax curtains).

Your three calls, captured

- Compass/morph → keep + refine, but confined strictly to the morph + rail-follow section; never floating over the door or the worlds.
- WhatsApp → re-skin into the warm palette (drops the recognizable green; say the word if you ever want it back). Footer emerald dot → gold.
- Mode → plan-first; you select below.

---
The plan

Bucket 1 — Remove the genuine AI-slop (the visible cohesion wins)

- P0 Kill the PlanetGlyph. Header.tsx:355 renders a literal ringed planet + orbiting dot next to "Mondes" — the single clearest "space theme" cliché on the ban list, and it miscasts your museum rooms as celestial bodies. Replace with a quiet glyph from the real metaphor (three stacked thresholds / an aperture wedge echoing the upper-left key light), or just keep "Mondes" + chevron.
- P1 Re-skin the WhatsApp FAB into the warm system: ink/void surface, thin brass ring, cream glyph, gold on hover, no glow (WhatsAppFab.tsx:23).
- P1 Footer "live status" chip emerald-400 + infinite animate-ping → gold/brass dot, single fade-in (ContactClose.tsx:148).
- P1 Testimonial card bg-white → bg-cream; warm the heavy black shadow toward void (Reviews.tsx:355).
- P1 Social tiles stop flooding full-saturation FB/IG/LinkedIn brand colors + 10% pop; cream icons, brass/gold ring on hover, quiet translate (ContactModal.tsx:127).
- P1 Delete two dead decorative blurs: backdrop-blur-xl behind a 95%-opaque dropdown (Header.tsx:175) and blur(1px) on paper tape (PaperArtifact.tsx:38). Zero visual loss.
- P1 Glow → lift: drop the cream pill-glow alpha 0.45 → ~0.30 (Header.tsx:216, CtaButton.tsx:25); soften the RunwayRule gold bloom.

Bucket 2 — Token foundation (mostly invisible, highest leverage against drift)

- P0 Add a borderRadius scale (sm/md/lg/xl/pill) and migrate the ad-hoc radii — CtaCard alone hardcodes four (CtaCard.tsx:26-38), plus WorldGallery/Reviews/ContactModal/PaperArtifact.
- P0 Self-host Satoshi via next/font/local; remove the render-blocking Fontshare @import (globals.css:3). Perf + privacy + unifies font loading with Didot.
- P1 Expose --cream-rgb / --gold-rgb / --void-rgb channel vars; rewrite the ~15 duplicated rgba(247,244,239…) / rgba(232,178,58…) / rgba(0,0,0…) alpha literals to reference them.
- P1 Collapse 6 bespoke near-blacks (#0a0c0f, #0b0e11, #0e1114, #0C1519, #1A1714, #14110C) and 5 paper-whites (#ECE7DD ×2, #F4F0E7, bg-white) onto tokens; add paper / briefingWall / museumWall.
- P1 Motion-duration tokens (micro .2s / entrance .7s / scroll 1.1s); reconcile Lenis 1.1 vs 1.2.

Bucket 3 — Typography discipline (visible consistency)

- P0 Take section numerals (I/II/III) off Didot at ~18px → Satoshi tabular-nums (Briefing StudioHero:230). Direct violation of "Didot ≥28px."
- P1 Raise every Didot clamp floor to 1.75rem (ContactModal:110, TransitionRope:249, StudioHero:239, Reviews:365, PourquoiNest:82, ContactClose:103).
- P1 Ship one <Kicker> on the currently-unused letterSpacing.kicker (.22em) token; replact each invent their own weight/color/size (FamilyCards:204, LaMethode:37, BriefingHero:245, CtaCard:49, WorldGallery:142). The most visible "not one system" tell.

Bucket 4 — Compass refinement + motion/code polish (your named concern + invisible polish)

- Confine the JourneyThread strictly to the voie→rail window: tighten the visibility gate so it never appears over the door/worlds, including after fast scroll or jumps (JourneyThread.tsx:76-111).
- Soften the morph: slow the compass→orb contraction, lower the halo/orb glow so it's a qith the photography.
- Token-derive the orb gradient (cream→gold) instead of the invented #FFF4D6/#F0C25A (JourneyThread.tsx:204); give z-[34] a named place in a z-scale.
- ease-editorial everywhere (replace browser-default ease in loupe/dropdown/social/world-ower4/power1 → power3/expo.
- CtaButton variants (inverted/size) to remove two !important hacks (CtaCard:67, ContactClose:93); rebind inline on-palette hexes to tokens (#C9612E→clay ActDoor:303).
- Mark the sanctioned single-hue write-on "gradient-text" reveals with a comment so a fut tidy stale comments; optionally delete orphaned Cursor.tsx (Loupe is the live cursor).
